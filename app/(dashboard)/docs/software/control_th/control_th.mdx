# การควบคุมโปรแกรม

## เมื่อเริ่มต้น (`on_start`)

รันโค้ดครั้งเดียวเมื่อโปรแกรมเริ่มต้น (`setup()`)

### Arduino Generates

```cpp
void setup() {
  // Code
}
```

---

## วนลูปตลอดเวลา (`on_loop`)

รันโค้ดซ้ำไปเรื่อยๆ ขณะที่โปรแกรมทำงาน (`loop()`)

### Arduino Generates

```cpp
void loop() {
  // Code
}
```

---

