# Program Control

## On Start (`on_start`)

Runs code once when the program starts (`setup()`).

### Arduino Generates

```cpp
void setup() {
  // Code
}
```

---

## On Loop (`on_loop`)

Runs code repeatedly while the program is active (`loop()`).

### Arduino Generates

```cpp
void loop() {
  // Code
}
```

---

